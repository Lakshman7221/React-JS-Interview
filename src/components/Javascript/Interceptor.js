import React from 'react'

const Interceptor = () => {
  return (
    <div>
    <div>
      <h3 style={{color:"purple"}}>Interceptor</h3>
      <p>
        <ul>
          <li>Particularly when working with APIs, interceptors are often used to intercept and modify HTTP requests and responses</li>
          <li>Modify HTTP requests and responses. By using request and response interceptors,</li>
          <li>We can add custom logic, modify headers, handle errors, transform data, and much more.</li>
        </ul>
      </p>
    </div>
  </div>
  )
}

export default Interceptor