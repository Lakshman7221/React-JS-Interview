import React from 'react'

const ArrowFunction = () => {
  return (
    <div>
    <div>
      <h3 style={{color:"purple"}}>Arrow Function</h3>
      <p>
        <ul>
          <li>The Arrow function is the new feature of the ES6 standard.</li>
          <li>Arrow functions are anonymous functions (the functions without a name and not bound with an identifier). They don't return any value and can declare without the function keyword.</li>
          <li>we can create components easily.</li>
          <li>It is also called 'fat arrow' {"(=>)"} functions.
          </li>
        </ul>
      </p>
    </div>
  </div>
  )
}

export default ArrowFunction